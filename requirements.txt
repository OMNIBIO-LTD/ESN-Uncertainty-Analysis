# omni3 — ESN Uncertainty for UR5
# Install with: pip install -r omni3/requirements.txt
# Requires Python 3.10 (LeRobot dependency)

# Core
numpy>=1.24
scipy>=1.10
torch>=2.0

# Dataset
lerobot>=0.4.0
huggingface-hub>=0.20

# Visualization
rerun-sdk>=0.26
matplotlib>=3.7

# Image handling (optional, for camera frames)
Pillow>=10.0
opencv-python>=4.8
